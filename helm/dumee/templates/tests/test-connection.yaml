apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dumee.fullname" . }}-test-connection"
  labels:
    {{- include "dumee.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dumee.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
